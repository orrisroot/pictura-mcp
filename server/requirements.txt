# Image generation MCP server deps (local GPU, low-VRAM friendly)
# Install:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#
# torch: >=2.9 so that CUDA-12.8-driver machines can pin 2.11.0+cu128
# (see requirements-cu128.txt). Default PyPI wheels (torch 2.14 = cu130 build)
# require a CUDA-13-capable driver (>=580).
torch>=2.9
diffusers>=0.40
transformers>=5.17
accelerate>=1.15
safetensors>=0.8
pillow>=12
mcp>=2.2
peft>=0.21
ultralytics>=8
