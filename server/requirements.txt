# Image generation MCP server deps (local GPU, low-VRAM friendly)
# Install:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#
# torch: >=2.9 (default PyPI wheel, torch 2.14 = cu130 build) requires a
# CUDA-13-capable driver (>=580). Volta/V100 machines need
# server/requirements-v100.txt (torch 2.7.1+cu126) instead.
torch>=2.9
diffusers>=0.40
transformers>=5.17
accelerate>=1.15
safetensors>=0.8
pillow>=12
mcp>=2.2
peft>=0.21
ultralytics>=8
python-multipart>=0.0.20
